# DEPRECATED - old block-accumulator bring-up note

This active-path filename is intentionally a deprecation stub. The previous
`rtl_next_step.md` policy described block-acc-only output and CPU-side scale
application, which conflicts with the current fixed-scale FPGA policy. Do not
use that policy for the final build path.

Historical copy:

- `docs/deprecated/rtl_next_step.md`

